- **模块化隔离架构**：采用 `test_<module>.py` 映射结构，利用 `unittest.mock` (MagicMock, AsyncMock) 深度隔离 OpenAI API 等外部依赖。
- **异步驱动机制**：基于 `pytest-asyncio` 驱动异步用例，配合 `fixture` 管理共享状态（如 Settings, ToolRegistry）。
- **分层验证策略**：涵盖从单类/函数的单元测试到 Agent 调用 FileTool 等组件协作的集成测试，确保配置加载至工具执行的完整逻辑闭环。