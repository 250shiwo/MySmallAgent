- 测试文件命名遵循 `test_<module_name>.py` 约定，与被测模块一一对应。
- 异步测试函数统一使用 `@pytest.mark.asyncio` 装饰器标记。
- 使用 `MagicMock(spec=SomeClass)` 创建具有类型约束的模拟对象，提高测试准确性。
- 对于涉及文件系统的测试，普遍使用 `tmp_path` fixture 创建临时目录和文件，避免污染真实文件系统。
- 测试类通常以 `Test<ClassName>` 命名，并包含 `setup_method` 或 fixture 来初始化被测对象。