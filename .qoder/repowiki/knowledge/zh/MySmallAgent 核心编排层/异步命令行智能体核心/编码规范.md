- 所有工具类均继承自 `tools.base.Tool` 抽象基类，并统一定义 `name`, `description`, `parameters` 和 `danger_level` 类属性。
- 工具的 `execute` 方法统一采用异步签名 `async def execute(self, **kwargs) -> str`，返回字符串形式的执行结果或错误信息。
- 模块内所有涉及 I/O 或网络请求的操作（如 LLM 调用、文件读写、Shell 执行）均采用 `async/await` 异步模式。