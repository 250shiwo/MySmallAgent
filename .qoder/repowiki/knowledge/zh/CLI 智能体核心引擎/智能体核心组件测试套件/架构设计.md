- 采用模块化测试结构，每个核心组件（agent, llm, config, tools）对应独立的测试文件，通过 `unittest.mock` (MagicMock, AsyncMock) 模拟 OpenAI API 等外部依赖。
- 利用 `pytest` 的 fixture 机制管理共享测试状态（如 Settings, ToolRegistry），并依赖 `pytest-asyncio` 支持异步测试用例。
- 包含单元测试（针对单个类/函数）和集成测试（验证组件间协作，如 Agent 调用 FileTool），确保业务逻辑闭环。