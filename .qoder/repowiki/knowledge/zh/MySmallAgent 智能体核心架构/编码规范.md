- 所有工具类必须继承自 `tools/base.py` 中的 `Tool` 基类，并实现异步 `execute` 方法。
- 持久化模块（Session/Memory）均采用“先写临时文件再重命名”的原子写策略，防止崩溃导致数据损坏。
- 技能定义遵循统一的 `SKILL.md` 格式，包含 YAML frontmatter 元数据与 Markdown 指令内容，由正则表达式解析而非引入重型 YAML 库。