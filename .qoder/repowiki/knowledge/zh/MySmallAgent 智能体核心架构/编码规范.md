- 所有涉及 I/O 的操作（LLM 调用、文件读写、Shell 执行）均采用 `async/await` 异步模式。
- 工具类继承自抽象基类 `Tool`，必须定义 `name`、`description`、`parameters` 和 `danger_level` 类属性，并实现异步 `execute` 方法。
- 工具执行结果统一返回 `str` 类型，错误信息通过捕获异常后格式化为字符串返回，而非抛出异常中断流程。
- 危险级别为 `dangerous` 的工具在执行前必须通过回调函数请求用户确认，`safe` 级别工具则自动执行。