- 采用模块化测试结构，每个核心组件（agent, llm, config, tools）对应独立的测试文件，利用 `unittest.mock` (MagicMock, AsyncMock) 隔离 OpenAI API 等外部依赖。
- 依赖 `pytest` 的 fixture 机制管理共享状态（如 Settings, ToolRegistry），并通过 `pytest-asyncio` 驱动异步测试用例。
- 涵盖单元测试（针对单个类/函数）与集成测试（验证 Agent 调用 FileTool 等组件协作），确保从配置加载到工具执行的完整业务逻辑闭环。